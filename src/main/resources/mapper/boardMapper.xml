<?xml version="1.0" encoding="UTF-8"?>
<!-- mapper 파일은 테이블당 최소 하나 생성 -->

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.ac.kopo.mapper.BoardMapper">
<!-- 여기에 CRUD(select, insert, update, delete) 코드 작성 -->

 	<select id="selectAll" resultType="kr.ac.kopo.board.vo.BoardVO">
		select no, title, writer, to_char(reg_date, 'yyyy-mm-dd hh:mi:ss') as regDate
		  from tbl_board
		 order by no desc
	</select>
	
	<insert id="insert" parameterType="kr.ac.kopo.board.vo.BoardVO">
		insert into tbl_board(no, title, writer, content)
		values(seq_tbl_board_no.nextval, #{title}, #{writer}, #{content})
	</insert>
</mapper>